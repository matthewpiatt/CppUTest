#include "CppUTest/mpiatt_logit.h"

#include <stdio.h>

void logit(const char* file,
           int line,
           const char* op,
           const char* callsite_file,
           int callsite_line,
           const char* data,
           size_t size)
{
//    if (!file) return;
//    if (!op) return;
//    if (!callsite_file) return;
//    if (!data) return;

    //printf("%s,%i,%s,%s,%i,\"%s\",%lu,\n", file, line, op, callsite_file, callsite_line, data, size);
}
